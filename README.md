# HarnessOS

个人 AI 编程 Harness 的单一真相源（Single Source of Truth）。实战产生原料，这里加工成规则，打包成 Skill 发布到各 AI 编程工具中运行。

## 三层流水线

```
notes/（原料区）  →  本仓库（加工厂）  →  发布产物
 随手记，2分钟一条      筛选、提炼、版本管理      skills/ → .skill 导入各 AI 工具
                                          global/ → ~/AGENTS.md 供所有 Agent 注入
```

- **notes/**：原始输入。踩坑记录、判断、实验结论。允许粗糙和矛盾，事后证明错了也没关系。唯一硬性要求：2 分钟内能记完，且带最低限度的结构（见下）。
- **skills/**：加工完成品。**只放自有 skill**，只含当前生效的规则，按场景拆分成单一职责的小 skill，靠 description 精准触发。
- **global/**：全局协作规则的源文件，发布为 `~/AGENTS.md`（如 `C:\Users\Administrator\AGENTS.md`）。只放跨项目通用、与具体仓库无关的规则；全局/项目的归属边界写在文件开头。注：Kimi Code 不注入全局规则，但此文件服务于所有注入它的 Agent，且「哪些规则归全局、哪些归项目」的边界本身需要一处维护——后续视情况把边界管理提炼为独立 skill。
- **vendor/**：引入的第三方 skill，原样保留，不做任何修改。上游更新时整目录替换，并在 CHANGELOG 记录来源与新版本。
- **加工是单向的**：永远改仓库里的源文件，绝不直接改已发布的产物（.skill、~/AGENTS.md）。

## 目录结构

```
├── notes/               # 原料区：踩坑笔记、观察记录
├── skills/              # 生产线：自有 Skill 源文件
│   ├── ai-stack-harness/    # AI 编程技术栈选型 Harness
│   └── ai-coding-workflow/  # AI 编程工作流 Harness
├── global/              # 全局规则：AGENTS.md 源文件，发布到 ~/AGENTS.md
├── vendor/              # 引入区：第三方 Skill（原样不改）
│   ├── keel/                # 架构治理协议（来源 lencx/skills）
│   └── coding-protocol/     # 风险分级编码执行协议（来源 lencx/skills）
├── scripts/             # 工具：pack.py 打包、publish_global.py 发布全局规则、dashboard.py 生成看板
├── dist/                # 打包输出（.skill 产物，不入库）
├── dashboard.html       # 规则资产看板（脚本生成，不手改）
└── CHANGELOG.md         # 规则加工历史
```

## 笔记的最低结构

notes/ 允许粗糙，但每条笔记必须带两行元信息，否则月度提炼时无法归并：

```markdown
> 日期：2026-07-23
> 场景：语言/工具/任务类型（如 rust、electron、长链路 agent 任务）
```

正文随意。「坑 + 当时判断 + 实际结果」是最有价值的组合。

### 声明式规则入口

除了归纳式踩坑笔记，notes/ 还接受「声明式规则」——owner 直接声明的意图型规则（工作习惯、工具偏好、验收标准等），加一行类型元信息：

```markdown
> 类型：声明式规则
```

与归纳式规则的分野：归纳式规则是事后归纳，需要三门槛防误提炼；声明式规则是明确意图，**豁免「重复 2 次」门槛，陈化期缩短为 ≥ 2 天**（防随口一说被永久固化），合入前须过一次**冲突检查**（对照现有 skill 与 global 规则，冲突时由 owner 裁决谁覆盖谁）。来源标注为 `owner-declared` 而非踩坑笔记；模型换代清理时，这类规则的废止标准是「owner 是否改主意」而非「场景是否失效」。

### 提议态：Agent 观察的转正通道

Agent 反复观察到 owner 纠正同一个问题、但 owner 从未明说的，可在 notes/ 记一条标 `> 类型：提议`。提议不是第三入口，是声明式规则的候选态：

```
Agent 观察 → notes 记「提议」→ 复述 → owner 确认 → 转声明式（确认日起算 2 天陈化）
                                  ↘ owner 否认 → 丢弃，或留作归纳式原料
```

**复述时机只有两个**（避免打断当前任务）：owner 主动在本仓库要求 Agent 自检时；每周评审时。提议在复述前不得直接固化为规则。

### 规则与技能的分界

提炼时用**失效测试**划界：去掉它，Agent 会「做错事」→ 约束规则；会「不会做某事」→ 技能。

| 维度 | 约束规则 | 技能 |
|---|---|---|
| 句式 | 「总是 / 绝不 / 优先 / 必须」 | 「当 X 时，按步骤 Y 做」 |
| 性质 | 判断和约束（防错） | 流程和方法（赋能） |
| 触发 | 常驻或按场景注入 | 按需调用（description 触发） |
| 内容 | 纯文字，几行以内 | 可含步骤、脚本、模板、checklist |
| 归属 | global/AGENTS.md 或 skill 内约束段 | skills/ 独立目录 |

一条笔记同时含两者时（如「验收六步法」是技能、「截图必须目检」是规则），提炼时拆开各走各路，不强行塞同一处。

## 规则的生命周期

每条规则有三种状态变更，全部记入 CHANGELOG，条目类型标注为 **新增 / 修订 / 废止**：

1. **新增**：notes/ 中的坑同时满足三个条件才提炼——**重复出现 2 次以上**、**陈化 ≥ 14 天**（防止把一次性情绪固化成规则）、**经过至少 1 次实战验证**。合入对应 skill，版本号 +1。**声明式规则例外**：豁免「重复 2 次」，陈化缩短为 **≥ 2 天**，合入前加做冲突检查（见「声明式规则入口」）。
2. **修订**：规则被实践证明不准确，改写法、改适用边界。版本号 +1。
3. **废止**：规则被证明错了，或因模型换代失效。**不删除，改写为废止块**，保留「为什么是错的」——这比规则本身更值钱：

```markdown
> **已废止（2026-08）：** 模型 X 代起此约束失效，原场景见 notes/2026-07-xxx。
> ~~原规则内容~~
```

## Skill 的生命周期

skill 整体也有生命周期，每月随提炼评审一并过一遍（看板提供数据）：

1. **使用热度**：`scripts/dashboard.py` 扫描各 Agent 会话日志（~/.claude/projects、~/.codex/sessions、~/.kimi/sessions），统计每个 skill 的近似使用次数与最后使用时间，显示在看板矩阵里。
2. **月度评审三问**：这个 skill 最近被用过吗？它的能力是否已被 Agent 原生功能取代（工具新增特性、模型变强）？它是否与其他 skill 或全局规则产生冲突？
3. **退役**：评审认定无存续价值的 skill 执行退役——从全部发布点撤下（`publish_skills.py` 的映射中移除后运行，并手动删除各池对应目录）、源目录移入 `archive/skills/`、CHANGELOG 记 **废止** 并写明理由。退役不可逆性低：归档仍在仓库里，随时可复活。
4. **零使用预警**：发布点日志中 90 天零使用的 skill 会在看板「待处理」中列出，提醒下次评审重点关注。

## 规则溯源

每条规则尽量标注来源笔记（一行即可），便于废止/清理时回溯原始场景：

```
来源：notes/2026-07-vibe-coding-观察.md
```

模型换代清理时，优先检查没有来源标注、且已想不起适用场景的规则。

## 运转节奏

1. **每次踩坑后**：往 notes/ 记一条，带上日期和场景两行元信息。
2. **每周评审（定时任务）**：每周日 21:12 自动跑——体检漂移、刷新使用热度与看板、列出满 14 天陈化期的可提炼笔记和 >90 天零使用的退役候选，摘要写入 `reviews/` 并提交 git。提炼与退役动作仍由人决定后手动执行。
3. **提炼（手动，跟随评审）**：按每周评审摘要，把满足三门槛（重复 2 次以上、陈化 ≥ 14 天、实战验证 1 次）的笔记提炼为规则；声明式规则按短通道（陈化 ≥ 2 天 + 冲突检查）提炼。合入对应 skill，标注来源（踩坑笔记或 owner-declared），版本号 +1，记入 CHANGELOG（新增/修订/废止）。
4. **每次大模型换代**：过一遍所有 skill，失效规则按「废止」处理，不直接删。
5. **发布**：改完任何规则后运行 `python scripts/sync.py` 一条命令完成全部——打包有改动的 skill、发布全局规则、发布 skill 目录、刷新看板；`sync.py --check` 只体检不写入。也可单独运行：`pack.py`（打包）、`publish_global.py`（全局规则）、`publish_skills.py`（skill 目录）。
6. **看板**：`dashboard.html` 浏览器打开即全局概览（单屏设计，细节回仓库查 CHANGELOG 与源文件）。

## 全局规则发布映射

| 目标 | 文件 | 内容 |
|---|---|---|
| home（通用） | `~/AGENTS.md` | 核心规则 |
| Codex | `~/.codex/AGENTS.md` | 核心规则 |
| OpenCode | `~/.config/opencode/AGENTS.md` | 核心规则 |
| Claude Code | `~/.claude/CLAUDE.md` | 核心规则 + `global/overlays/claude.md`（子智能体、Git 纪律、代码语言等专属规则） |
| Kimi Code | — | 无全局注入机制，不发布；规则走项目级 AGENTS.md / skills |

`publish_global.py --check` 只检查漂移不写入；覆盖前自动备份到 `backups/`（不入库）。

## 引入 Skill 来源

- vendor/keel、vendor/coding-protocol 来自 https://github.com/lencx/skills ，原样引入未做修改，感谢作者 lencx。
