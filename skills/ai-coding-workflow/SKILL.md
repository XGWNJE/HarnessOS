---
name: ai-coding-workflow
version: 2.0.0
description: AI 编程工作流与选型判断基线（来源索引）。工作流与选型的原则、验收标准与红线已上移浓缩进 global/AGENTS.md 的「AI 协作与工程判断基线」一节。触发本 skill 时以全局 Rules 为准；本文件为 Harness 来源出处与历史入口，不再单独维护正文。
---

# AI 编程工作流 Harness（已并入全局规则）

产出以验证增量计量、模型选型基于真实任务证据、复杂任务靠交叉验证、封装层诚实失败、AI 输出当待验证线索、重写以新底座可验证为前置等基线，已浓缩进 `global/AGENTS.md`「AI 协作与工程判断基线」一节，此处不再重复维护。

触发本 skill 时请以全局 Rules 为准。

> 历史：本正文 v1.x 曾在本地维护完整工作流 Harness；现其原则性内容已上移为跨项目全局规则。若未来需要更细的流程化做法，再在此派生对应 skill。
