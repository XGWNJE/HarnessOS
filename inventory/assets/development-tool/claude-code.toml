schema_version = 1
id = "development-tool:claude-code"
type = "development-tool"
name = "Claude Code"
domain = "ai-agent"
status = "managed"
purpose = "提供 Anthropic Claude 的终端 AI 编程代理入口。"
platforms = ["windows"]
fact_source = "inventory/assets/development-tool/claude-code.toml"
preference_source = "owner-declared"
provenance = "third-party"
upstream_updates = true
declared_on = "2026-09-04"
last_verified_on = "2026-09-06"
review_days = 90
notes = "当前通过 npm 全局安装 @anthropic-ai/claude-code 2.1.260；npm `latest` 为 2.1.261，`stable` 标签为 2.1.236，`next` 为 2.1.263；该包以 Node/npm 完成安装，但当前 claude.cmd 启动包内原生 claude.exe。记录 Node 依赖用于复刻当前安装形态；账号、API key 与会话不纳入仓库。"

[[relationships]]
type = "depends-on"
target = "development-tool:nodejs"

[development_tool]
tool_kind = "cli"
commands = ["claude"]
install_source = "npm 官方 registry"
package_id = "@anthropic-ai/claude-code"
version_constraint = ">=2.1.260"
update_channel = "other"
environment_variables = ["PATH"]
config_reference = "Claude Code 用户级配置与操作系统凭据存储"
verification_commands = ["claude --version", "npm ls -g @anthropic-ai/claude-code --depth=0"]
